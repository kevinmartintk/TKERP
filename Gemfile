source 'https://rubygems.org'


#tut gems calendar
gem 'rabl', '~> 0.9.3'
gem 'simple_form', '3.1.0.rc1'
gem 'bootstrap-sass', '~> 3.1.1'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.0'

# Use postgresql as the database for Active Record
gem 'pg', '~> 0.18'

# PgSearch builds Active Record named scopes that take advantage of PostgreSQL's full text search
gem 'pg_search', '~> 1.0.5'

# Use SCSS for stylesheets
gem 'sass-rails', '5.0.2'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '2.7.0'

# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '4.1.0'

# Use jquery as the JavaScript library
gem 'jquery-rails', "~>4.0.0"

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks', '2.5.3'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '2.2.6'

# Flexible authentication solution for Rails with Warden
gem 'devise','3.4.1'

# Strategy to authenticate with Google via OAuth2 in OmniAuth.
gem "omniauth-google-oauth2", '0.2.6'

# Destroy on an Active Record object that it didn't actually destroy
gem "paranoia", "~> 2.0"

# jQuery plugin for drop-in fix binded events problem caused by Turbolinks
gem "jquery-turbolinks", "~> 2.1.0"

gem 'jquery-ui-themes'

# Paperclip is intended as an easy file attachment library for Active Record
gem 'paperclip', '~> 4.2.1'

#Add width, height, and size to paperclip images.
gem 'paperclip-meta', '~>1.1.0'

# jquery plugin for validation in rails project.
gem "jQuery-Validation-Engine-rails", "~> 0.0.2"

# Plugin for creating and managing a breadcrumb navigation for a Rails project.
gem "breadcrumbs_on_rails", '2.3.0'

# Authorization solution for Rails which is decoupled from user roles.
gem 'cancancan', '~> 1.10'

# FriendlyId lets you create pretty URLs and work with human-friendly strings as if they were numeric ids.
gem 'friendly_id', '~> 5.1.0'

# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '0.4.1', group: :doc

# We can add a datepicker with this gem
gem 'bootstrap-datepicker-rails'

# We can use this gem to make a field 'autocompletable'
gem 'rails4-autocomplete', '~> 1.1.1'

gem 'jquery-ui-rails', '~> 5.0.0'

#a gem to validate countries
gem 'country-select', '~> 1.1.1'

gem 'momentjs-rails', '~> 2.9',  :github => 'derekprior/momentjs-rails'
#Gem for datetimepicker
gem 'datetimepicker-rails', github: 'zpaulovics/datetimepicker-rails', branch: 'master', submodules: true

# Use Schema Plus for generating foreign keys automatically
gem 'schema_plus', '~> 1.8.7'

# Use Seed Bank for having more than one seed file and generating data runing tasks
gem 'seedbank', '~> 0.3.0'

# Use Will Paginate for performing paginated queries
gem 'will_paginate', '~> 3.0.7'

# Gem to conveniently handle multiple models in a single form with Rails 3 and jQuery or Prototype.
gem 'nested_form', '~> 0.3.2'

# Simple, Heroku-friendly Rails app configuration using ENV and a single YAML file
gem 'figaro', '~> 1.1.0'

# Prawn is a fast, tiny, and nimble PDF generator for Ruby
gem 'prawn', '~> 2.0.1'

# Support .prawn templates as Prawn::Document content
gem 'prawnto', '~> 0.1.1'

# Prawn::Table provides tables for the Prawn PDF toolkit
gem 'prawn-table', '~> 0.2.1'

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug', '3.5.1'
  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '2.0.0'
  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring', '1.2.0'
end

group :development do
  # gem 'better_errors', '2.1.1'
  gem 'binding_of_caller', '0.7.2'
  gem 'bullet', '4.14.4'
  gem 'rails_best_practices', '1.15.6'
end


# push test